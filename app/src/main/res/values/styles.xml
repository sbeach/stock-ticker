<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/stock_ticker_green</item>
        <item name="colorPrimaryDark">@color/stock_ticker_green_dark</item>
        <item name="actionModeStyle">@style/StockTicker.ActionMode</item>
    </style>

    <style name="SettingsTheme" parent="AppTheme">
    </style>

    <style name="StockTicker.ActionMode" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@color/stock_ticker_green_action_mode</item>
    </style>

</resources>
